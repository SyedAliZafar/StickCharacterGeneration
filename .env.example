# Copy to .env and fill in your keys
# (These are already in videoSequence/.env — just copy that file here)

OPENAI_API_KEY=sk-proj-...          # DALL-E 3 image generation
OPENROUTER_API_KEY=sk-or-v1-...    # DeepSeek-V3 (grouping + prompts)
